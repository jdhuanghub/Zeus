Hello World! This is jdwong's repository.
Some of my projects are listing in this repository.

Git commands:
"git add "  //add files to stage.
"git commit"  //commit files to branch.

Scene01: While accidently modified sth in working directory! Use: "git checkout --xxxx"  //xxxx is file name, undo a recent change of a file in working directory.
Scene02: While accidently modified sth and add to stage area, Use: "git reset HEAD file"  //unstage a recent change from stage area.
Scene03: While you do all above things and commit to repository! Use: " git reset --hard commmit_id"  //go back to previous version(commit_id) of file.

git log   //see commit history
git reflog   //see command history

Creating and merging branch:
git branch   //see branch
git branch <name>  //create branch
git checkout <name> //switch to branch <name>
git checkout -b <name>  //create and switch branch
git merge <name>   //merge a branch to current branch
git branch -d <name>   //delete branch

